.secondary-menu.row
  .col-md-5.col-sm-3.col-xs-6
    = link_to bot_dashboards_path(@bot), class: 'breadcrumb' do
      %h2 All Metrics
    %span.breadcrumb-separator
      \/
    %h2 New Dashboard

.container-fluid.graph-container
  .graph.row
    .col-md-6.col-sm-6.col-xs-6.graph-container
      = simple_form_for [@bot, @dashboard], url: bot_dashboards_path, method: :post do |f|
        = f.input :name, label: "Your Custom Dashboard's Name", required: true
        = f.input :regex, label: "Regular Expression To Match Messages Against", required: true
        %p.disclaimer
          For e.g. if your bot has a failure condition when it responds with "I don't know what you mean", set the regex as
          %strong
            don't know what you mean
        = f.submit "Create New Dashboard"

