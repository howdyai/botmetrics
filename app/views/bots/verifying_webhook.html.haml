.row
  .col-md-4.col-md-offset-4
    %h3.text-center
      Verifing webhook of #{@bot.name}
    %p.intro
      We are verifying webhook for your bot:
    .progress.progress-striped.active
      .progress-bar.progress-bar-success{ role:'progressbar', 'aria-valuenow' => '50', 'aria-valuemin' => '0', 'aria-valuemax' => '100', style: "width: 50%" }

    .setup-done
      %p.verified-failed Failed to verify webhook, please retry.

      %p.verified-success
        We've setup webhook for your bot. You can go to your dashboard to start analyzing how your bot is doing.

      .go-to-settings.text-center
        = link_to 'Go Back To Settings', edit_bot_path(@bot), class: 'btn btn-lg btn-success'

- content_for :page_scripts do
  :javascript
    App.page = new App.VerifyingWebhook(#{@bot.id.to_json}, #{Settings.pusher_api_key.to_json});
    App.page.run();
